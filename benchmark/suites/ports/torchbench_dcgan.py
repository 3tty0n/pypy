# -*- coding: utf-8 -*-
# Generated by benchmark/suites/port.py from torchbenchmark.models.dcgan
# (torchbenchmark git 0fff8bf354, source sha256 fa4f1a563e75dd6e).  Do not edit; change the spec.
# Kept: DCGAN, Discriminator
from __future__ import absolute_import, division, print_function
import torch.nn as nn

class DCGAN:

    def __init__(self, bench):
        self.image_size = 64
        self.nc = 3
        self.nz = 100
        self.ngf = 64
        self.ndf = 64
        self.num_epochs = 5
        self.lr = 0.0002
        self.beta1 = 0.5
        self.ngpu = 1
        self.device = bench.device

class Discriminator(nn.Module):

    def __init__(self, ncgan):
        ngpu = ncgan.ngpu
        nc = ncgan.nc
        ndf = ncgan.ndf
        super(Discriminator, self).__init__()
        self.ngpu = ngpu
        self.main = nn.Sequential(nn.Conv2d(nc, ndf, 4, 2, 1, bias=False), nn.LeakyReLU(0.2, inplace=True), nn.Conv2d(ndf, ndf * 2, 4, 2, 1, bias=False), nn.BatchNorm2d(ndf * 2), nn.LeakyReLU(0.2, inplace=True), nn.Conv2d(ndf * 2, ndf * 4, 4, 2, 1, bias=False), nn.BatchNorm2d(ndf * 4), nn.LeakyReLU(0.2, inplace=True), nn.Conv2d(ndf * 4, ndf * 8, 4, 2, 1, bias=False), nn.BatchNorm2d(ndf * 8), nn.LeakyReLU(0.2, inplace=True), nn.Conv2d(ndf * 8, 1, 4, 1, 0, bias=False), nn.Sigmoid())

    def forward(self, input):
        return self.main(input)
