# -*- coding: utf-8 -*-
# Generated by benchmark/suites/port.py from torchbenchmark.models.pyhpc_turbulent_kinetic_energy
# (torchbenchmark git 0fff8bf354, source sha256 f9a48a05390f647d).  Do not edit; change the spec.
# Kept: TurbulentKineticEnergy
from __future__ import absolute_import, division, print_function
import torch
import torchbench_tke_pytorch as tke_pytorch

class TurbulentKineticEnergy(torch.nn.Module):

    def __init__(self, device):
        super(TurbulentKineticEnergy, self).__init__()
        self.device = device

    def forward(self, u, v, w, maskU, maskV, maskW, dxt, dxu, dyt, dyu, dzt, dzw, cost, cosu, kbot, kappaM, mxl, forc, forc_tke_surface, tke, dtke):
        return tke_pytorch.integrate_tke(u, v, w, maskU, maskV, maskW, dxt, dxu, dyt, dyu, dzt, dzw, cost, cosu, kbot, kappaM, mxl, forc, forc_tke_surface, torch.clone(tke), torch.clone(dtke))
